{
  "name": "@conjecture/refute",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Counterexample search over exact integer arithmetic. Real evaluation, no floating point, no eval().",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf dist tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "@conjecture/core": "1.0.0"
  }
}
